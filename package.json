{
  "name": "github-management",
  "version": "1.0.0",
  "description": "Manage your GitHub account from a local visual UI — follow relationships (unfollow / follow back) and repository visibility (public ↔ private). Auth via the local GitHub CLI.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "swagger-ui-express": "^5.0.1"
  }
}
